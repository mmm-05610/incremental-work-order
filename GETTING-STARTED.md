# 上手：在你自己的项目里跑这套派工流程

这份是给人看的。目标：**半小时内跑通一次"初始化 → 派单 → 起执行者 → 用检查点 → 合并"**。

## 你会用到的四样东西

| 对象 | 是什么 | 谁动它 |
| --- | --- | --- |
| **主树 + 调度者** | 你对话的那个 agent：写单、投递、观察、合并 | 你 ⇄ 它 |
| **调度权威** | 主树 `docs/implementation/` 下的 `README.md`（规则）/ `manifest.json`（队列）/ `status.md`（汇总账）/ `rulings.md`（裁决账） | 调度者写；执行者只读 |
| **工单** | 一张自包含、可提交、可验收的增量任务；**契约权威在子树的 `work-orders/`** | 调度者写并投递 |
| **执行者** | 另一个会话，在它自己的工作树里跑；连续干活，批末打检查点后继续 | 执行者自己；**你只在想看时看** |

三条铁律：

- **一棵树（一个队列切片）同时只有一个执行者**；不同树可以并行。
- **开新工作树 / 新执行者要你通过；合并回主树要你批准**。
- 执行者**不会为检查点停下**——检查点是给你看进度的窗口，不是审批闸门。

## 前提（1 分钟）

- 项目是 git 仓库（流程靠提交、baseline、tag）。
- skill 只留一份目录（用户级 `~/.agents/skills/` 或项目内 `.agents/skills/`——同名会互相遮蔽）。
- 第一次别拿最关键的东西试：挑一件**你已经想清楚**的小改动。

## 第 1 步：初始化（只建调度侧，不派单）

```text
这个仓库还没有调度系统。按 incremental-work-order 初始化：
建立 docs/implementation/{README.md, manifest.json, status.md, rulings.md, prefs.md, executor-charter.md}，
manifest 里 orders 与 executors 都是空的（派单与开树时才填）；rulings.md 第一条记"采用本流程"。
所有现状事实必须实测（带 文件:行 或实测值）；status.md 首条写实测基线（提交 sha + 跑一次构建/测试的计数与退出码）。
项目已有计划文档就只记指针、不另建不改它。不改业务代码、不碰 AGENTS.md、只显式 stage 这几个文件并提交。
建完给我一屏报告，并从实测里提 1–2 个候选让我挑。
```

**验收**：不看聊天记录，只看那几个文件就能说清"项目现状、下一步做什么、谁在做"。

## 第 2 步：派第一张单

```text
我想做 <结果>。按 incremental-work-order 判断能不能派；不能派就告诉我还缺哪个决定（设计未决不许派）。
能派的话：用它的模板写单、投递到 <子树>、在 manifest 里登记，并在回复里给我启动材料。
```

重点看三处：**现状是实测还是猜的**；**Gates 四列齐不齐**（门名 / 断言 / 反例 / 缺席时行为）；**写权边界**是否明确。

## 第 3 步：开树 + 起执行者（都需要你）

调度者先给你一份**开树提议**（为什么独立、落点与分支、基线、写权范围、第一批单、批次边界、预计成本）——你点头后它建树、写章程、投递第一批单，然后**在回复里直接给你启动提示词全文**。
**你**开一个新会话（工作目录 = 那棵子树），把提示词粘进去。就这样，剩下的它会自己跑。

## 第 4 步：检查点怎么用（你想看就看）

执行者做完一批会在它自己树上打 `checkpoint/<批次名>` 并更新它的 `status.md`，**然后继续干活**。

```bash
git -C <子树> tag -l 'checkpoint/*'          # 有哪几个检查点
cat <子树>/docs/implementation/status.md      # 能试什么 / 要你拍的 / 花了什么 / 恢复点
```

**亲自跑一遍它说能试的东西**——新问题基本都是这样冒出来的。要它改方向/加活，跟调度者说，调度者按"追加队尾 / 插队 / 改向"处理（**不打断当前批次**）。

## 第 5 步：什么时候合并

批次末 + 它那棵树干净 + 门刚跑过 → 调度者**提合并**（含变更面、冲突预判、合并后重验方法、回滚路径）→ **你批** → 主树 `merge --no-ff` → **在主树上重跑门与全量套件、重算摘要**（不沿用执行者数字）→ 记账 → 更新其他树的基线。多个执行者**一次合一个**。

## 第 6 步：什么时候开第二棵树

判据只有一个：**剩下的活里有一块与现有执行者写权面不重叠、且已经设计定型**。有 → 让调度者提方案；没有 → 别开（协调与合并成本大于并行收益）。

## 新手最容易踩的五个坑

1. **把讨论稿当工单**：设计未定时派单 ⇒ 执行者替你发明产品语义（最贵的错误）。
2. **以为检查点会停下**：不会——它是给你看的窗口；要改方向得主动跟调度者说。
3. **同一个队列切片挂两个执行者**：两人抢同一张单。
4. **只扩不并**：分支开了一堆、没并回主树。
5. **只看"全绿"**：绿要带反例和"缺席行为"，否则可能是假绿（见 `references/false-green-checklist.md`）。
